// Document service placeholder
export const documentService = {
  // Placeholder implementation
  async indexDocument(index: string, document: any) {
    return { success: true, index, document };
  }
};